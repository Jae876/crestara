# Neon PostgreSQL (Cloud Database)
# Get your DATABASE_URL from: https://console.neon.tech/app/projects

DATABASE_URL="postgresql://user:password@ep-xyz.us-east-1.aws.neon.tech/crestara?sslmode=require"

# Server
PORT=3001
NODE_ENV=production

# JWT (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION=3600
REFRESH_TOKEN_EXPIRATION=604800

# CORS  
CORS_ORIGIN=https://yourdomain.com

# Redis (Upstash for production)
REDIS_URL="redis://default:token@host.upstash.io:port"
